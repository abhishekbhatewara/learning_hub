# Copy this file to ".env" (which is git-ignored) and fill in the real values.
# The service-role key is a SECRET that bypasses Row-Level Security — never commit it,
# never paste it into chat. If it ever leaks, rotate it in the Supabase dashboard.

SUPABASE_URL=https://utlpogmahqmswrffzkjc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-the-service-role-key-here
